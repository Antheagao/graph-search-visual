import random 

def create_grid(mode: str, rows: int | None, cols: int | None, start: tuple[int, int] | None, end: tuple[int, int] | None):
    if mode == "custom":
        # Generate grid with random results
        pass
    else: 
        return None